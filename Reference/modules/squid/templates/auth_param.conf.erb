# Squid Auth configuration
<% 
if scope.lookupvar('squid::params::auth_enabled_type') == "basic"
	autharray.sort_by {|key, value| key.to_i}.each do |key, value|
-%>
auth_param <%= value['type'] %> <%= value['setting'] %>
<%
	end
end
-%>